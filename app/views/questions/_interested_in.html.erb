<%= form_with(model: current_user.interests.build, local: false ) do |f| %>
  <div><%= hidden_field_tag :question_id, @question.id %></div>
  <div class="question__interest-btn">
    <%= f.submit "知りたい!"%>
    <%= image_tag 'interest.svg', :alt => 'デフォルト画像' %>
  </div>
<% end %>

